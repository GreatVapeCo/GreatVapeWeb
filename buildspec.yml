version: 0.2

phases:
  install:
    commands:
      - echo No commands...
  pre_build:
    commands:
      - echo No Pre...
  build:
    commands:
      - echo Build started on `date`
      - echo Compiling the Node.js code
      - npm install
  post_build:
    commands:
      - echo Build completed on `date`
artifacts:
  files:
    - app.js
